[package]
name = "bebop-p2e"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib", "cdylib"]

[features]
build-bitstream = []

[dependencies]
libc = "0.2"
once_cell = "1.19"
snafu = "0.8"
log = "0.4"
